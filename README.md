# Project_2_price_compare 🛒

Веб-приложение на Django для мониторинга и сравнения цен на товары в популярных торговых сетях — **«Пятёрочка»** и **«Магнит»**. Сервис позволяет находить выгодные предложения, сопоставляя одинаковые товары в разных магазинах.

## 🚀 Основные возможности

*   **Автоматизированный парсинг**: Сбор данных с сайтов ритейлеров с использованием Selenium (в headless-режиме) и BeautifulSoup4.
*   **Умное сопоставление (Smart Matching)**: Алгоритм на базе библиотеки `fuzzywuzzy` (расстояние Левенштейна) находит идентичные товары даже при различиях в названиях.
*   **Личный кабинет**: Регистрация и авторизация пользователей для работы со списком избранных товаров.
*   **Каталог и категории**: Гибкая система распределения товаров по категориям с отслеживанием актуальности данных (времени последнего парсинга).
*   **Корзина**: Возможность собирать список покупок и видеть общую разницу в цене между магазинами.
*   **Логирование**: Система логирования процесса парсинга и ошибок в консоль и файлы для удобной отладки.

## 🛠 Стек технологий

*   **Язык**: Python 3.x
*   **Фреймворк**: Django 6.0
*   **Парсинг**: Selenium, BeautifulSoup4, webdriver-manager
*   **Сравнение данных**: FuzzyWuzzy
*   **База данных**: SQLite (по умолчанию) / PostgreSQL
*   **Тестирование**: Pytest, Pytest-django

## 📋 Установка и запуск

1.  **Клонируйте репозиторий**:
    ```
    git clone https://github.com/Adelina20034/Project_2_price_compare.git
    cd Project_2_price_compare
    ```

2.  **Настройте виртуальное окружение**:
    ```
    python -m venv venv
    # Для Windows:
    venv\Scripts\activate
    # Для macOS/Linux:
    source venv/bin/activate
    ```

3.  **Установите зависимости**:
    ```
    pip install -r requirements.txt
    ```

4.  **Выполните миграции**:
    ```
    python manage.py migrate
    ```

5.  **Запустите сервер**:
    ```
    python manage.py runserver
    ```

## 🔍 Использование парсера

Обновление данных в базе запускается через консольную команду Django. Парсер автоматически обходит защиту сайтов и сохраняет результаты в базу данных.

